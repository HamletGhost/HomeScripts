#
# User base environment settings
# Sourced by user's .bashrc
#

[[ -d "${HOME}/programs/bin" ]] && AddToPath PATH "${HOME}/programs/bin"
[[ -d "${HOME}/programs/lib" ]] && AddToPath LD_LIBRARY_PATH "${HOME}/programs/lib"

