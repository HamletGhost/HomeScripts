# resets the LINES and COLUMNS variables of the terminal,
# only if not a login shell
if [[ -z "$LINES" ]] || [[ -z "$COLUMNS" ]] && ! shopt -q login_shell ; then
	which resize >& /dev/null && eval $(resize)
fi
