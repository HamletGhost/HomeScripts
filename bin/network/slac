#!/usr/bin/env bash

# sourcing is important to have the script name propagated
source sshell --autohost='slac' --domain='slac.stanford.edu' --user='petrillo' --jump='jump.slac.stanford.edu' --noticket "$@"
# source sshell --autohost='slac' --domain='slac.stanford.edu' --user='petrillo' --k5realm='SLAC.STANFORD.EDU' "$@"
