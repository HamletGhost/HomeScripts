#!/usr/bin/env bash

# sourcing is important to have the script name propagated
source sshell --autohost='sshlac' --domain='slac.stanford.edu' --user='petrillo' --jump='s3dflogin.slac.stanford.edu' "$@"
